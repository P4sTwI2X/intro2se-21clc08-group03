class OrderController {
    postUpdateOrders(orderId, updatedData) {
      // Xử lý logic cập nhật đơn hàng
    }
  
    postRemoveOrders(orderId) {
      // Xử lý logic xóa đơn hàng
    }
  
    postAddOrders(orderData) {
      // Xử lý logic thêm đơn hàng
    }
  
    postUpdateOrders_Shop(shopId, orderId, updatedData) {
      // Xử lý logic cập nhật đơn hàng của cửa hàng
    }
  
    postAddOrders_Shop(shopId, orderData) {
      // Xử lý logic thêm đơn hàng cho cửa hàng
    }
  
    postRemoveOrders_Shop(shopId, orderId) {
      // Xử lý logic xóa đơn hàng của cửa hàng
    }
  }
